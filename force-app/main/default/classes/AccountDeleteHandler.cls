/*The following trigger will prevent the users from deleting the Accounts. This is because System Administrator has all the permissions,
we cannot change the permissions.*/
public class AccountDeleteHandler {
   // public static void deleteAct(List<Account> delAct){
        
     //   for(account act: delAct){
     //   act.addError('Please do not delete the account');
     //   }
    //}

}